<!DOCTYPE html> <!-- Define el estándar de HTML5 -->
<html lang="en"> <!-- Especifica que el idioma de la página es inglés -->

<head> <!-- Contiene información meta y configuración -->
    <meta charset="UTF-8"> <!-- Asegura que los caracteres especiales se muestran correctamente -->
    <meta name="viewport" content="width=device-width, initial-scale=1.0"> <!-- Ajusta la vista en dispositivos móviles -->
    <title>Document</title> <!-- Título que aparece en la pestaña del navegador -->
</head>

<body> <!-- Aquí empieza el contenido visible en la página -->
    <p> <!-- Párrafo con información sobre blockquote -->
        The <strong>HTML <code>&lt;blockquote&gt;</code> Element</strong> (or
        <em>HTML Block Quotation Element</em>) indicates that the enclosed text is an extended quotation.
    </p>

    <p>Here is a blockquote:</p> <!-- Introduce un ejemplo de blockquote -->

    <blockquote cite="https://developer.mozilla.org/en-US/docs/Web/HTML/Element/blockquote"> <!-- Cita en bloque con referencia -->
        <p> <!-- Contenido de la cita -->
            The <strong>HTML <code>&lt;blockquote&gt;</code> Element</strong> (or
            <em>HTML Block Quotation Element</em>) indicates that the enclosed text is an extended quotation.
        </p>
    </blockquote>

    <p> <!-- Explicación del elemento de cita corta -->
        El elemento de cita — <code>&lt;q&gt;</code> — se
        <q cite="https://developer.mozilla.org/es/docs/Web/HTML/Element/q">utiliza en para citas breves que no requieren saltos de párrafo.</q>
    </p>
</body>

</html> <!-- Cierre del documento HTML -->
